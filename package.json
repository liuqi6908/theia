{
  "private": true,
  "name": "theia",
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "scripts": {
    "build": "pnpm --filter electron-app bundle",
    "start": "pnpm --filter electron-app start",
    "watch": "pnpm --filter extension --filter electron-app --parallel watch"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  }
}
