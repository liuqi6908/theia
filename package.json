{
  "private": true,
  "name": "theia",
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "scripts": {
    "build": "pnpm --filter theia-electron bundle",
    "start": "pnpm --filter theia-electron start",
    "watch": "pnpm --filter theia-extension-* --filter theia-electron --parallel watch"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  }
}
